import { darkTheme } from '../styles';

const Footer = () => {
    return (
        <footer className={`${darkTheme()} p-4 text-center`}>
            Regynald Zambrano
        </footer>
    );
};

export default Footer;
