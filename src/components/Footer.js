import { THEME } from '../styles';

const Footer = () => {
    return (
        <footer className={`${THEME} p-4 text-center`}>
            Regynald Zambrano
        </footer>
    );
};

export default Footer;
